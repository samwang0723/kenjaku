server:
  host: "127.0.0.1"
  port: 8080

redis:
  url: "redis://localhost:6379"

telemetry:
  log_level: "debug"

# Phase 3e: tenancy is always on. Dev keypair provisioned via `make dev-setup`.
tenancy:
  jwt:
    issuer: "kenjaku-dev"
    audience: "kenjaku-api"
    public_key_path: "config/dev/public.pem"
